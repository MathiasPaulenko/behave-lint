# Minimal Example

A minimal Behave project demonstrating basic `behave-lint` usage.

## Structure

```text
minimal/
  features/
    login.feature
  behave-lint.toml
```

_Implementation pending — example files will be added in M4._
