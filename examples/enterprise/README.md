# Enterprise Example

A large-scale Behave project demonstrating enterprise `behave-lint` usage
with custom configuration, CI integration, and multiple feature suites.

_Implementation pending — example files will be added in M4._
